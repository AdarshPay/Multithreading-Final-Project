1
cdw or cd $WORK to get to your working directory

2
idev -p rtx
export PATH=$PWD/../../julia-1.9.3/bin:$PATH

3
module load python3/3.9.2
module load cuda/10.1

4
Clone the repo

5
cd Multithreading-Final-Project

6
python3 -m venv .venv   // Ensure to use python3 and not python

7
source .venv/bin/activate

8
pip install --upgrade pip
pip install -r requirements.txt

9
deactivate // deactive environment when done



 To Run Julia in a Virtual Environment:
- Create the virutal environment
- pip install julia (inside the virtual environment)
- then start python3 and run the following lines:
    - import julia
    - julia.install() <-- This line will build PyCall as well
